package com.systemmonitor.notification

enum class NotificationType {
    SECURITY,
    DEVICE,
    VAULT,
    APPLOCK,
    NETWORK
}

enum class NotificationPriority {
    LOW,
    DEFAULT,
    HIGH
}
